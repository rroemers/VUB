;ex 1.3: Define a procedure that takes three numbers as arguments and returns the sum of the squares
;of the two larger numbers.

;define procedure
(define (sum-of-sq a b c)
;sort for largest/smallest nr
  ((define high a) (define low b)
                   (cond (and? (> high low) (> c low) (display a))
                         (else (display b)))))
;define square procedure
;sum squares of results
